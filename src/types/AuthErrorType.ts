export type LoginErrorType = {
    email?: string;
    password?: string;
};

export type RegisterErrorType = {
    firstName?: string;
    lastName?: string;
    email?: string;
    password?: string;
    confirmPassword?: string;
};
