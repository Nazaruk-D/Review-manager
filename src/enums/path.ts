export enum Path {
    Root = '/',
    Login = '/login',
    Register = '/register',
    Profile = '/profile/:userId',
    Review = '/review/:reviewId',
    CreateReview = '/create-review/:userId',
}
